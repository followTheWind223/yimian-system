<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yimian.system.mapper.CommentMapper">

    <sql id="Base_Column_List">
        id, target_type, target_id, parent_id, reply_to_comment_id, reply_to_user_id,
        author_id, content, like_count, reply_count, status, created_at, updated_at
    </sql>

    <sql id="Not_Deleted">
        (deleted IS NULL OR deleted = 0)
    </sql>

    <select id="selectRootComments" resultType="com.yimian.system.entity.Comment">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_comment
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          AND parent_id IS NULL
          AND status = 1
          AND <include refid="Not_Deleted"/>
        <choose>
            <when test="sort == 'newest'">
                ORDER BY created_at DESC
            </when>
            <otherwise>
                ORDER BY (COALESCE(like_count, 0) * 3 + COALESCE(reply_count, 0) * 2) DESC,
                         created_at DESC
            </otherwise>
        </choose>
    </select>

    <select id="selectRepliesByParentIds" resultType="com.yimian.system.entity.Comment">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_comment
        WHERE parent_id IN
        <foreach collection="parentIds" item="parentId" open="(" separator="," close=")">
            #{parentId}
        </foreach>
          AND status = 1
          AND <include refid="Not_Deleted"/>
        ORDER BY created_at ASC
    </select>

    <update id="updateLikeCount">
        UPDATE sys_comment
        SET like_count = GREATEST(COALESCE(like_count, 0) + #{delta}, 0)
        WHERE id = #{id}
          AND <include refid="Not_Deleted"/>
    </update>

    <update id="updateReplyCount">
        UPDATE sys_comment
        SET reply_count = GREATEST(COALESCE(reply_count, 0) + #{delta}, 0)
        WHERE id = #{id}
          AND <include refid="Not_Deleted"/>
    </update>

    <select id="countByTarget" resultType="int">
        SELECT COUNT(1)
        FROM sys_comment
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          AND status = 1
          AND <include refid="Not_Deleted"/>
    </select>
</mapper>
