<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yimian.system.mapper.TagMapper">

    <sql id="Not_Deleted">
        (deleted IS NULL OR deleted = 0)
    </sql>

    <select id="selectByIds" resultType="com.yimian.system.entity.Tag">
        SELECT * FROM sys_tag
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND <include refid="Not_Deleted"/>
    </select>

    <select id="countApprovedUsageByTagId" resultType="Long">
        SELECT COUNT(DISTINCT k.id)
        FROM sys_knowledge_tag kt
        INNER JOIN sys_knowledge k ON k.id = kt.knowledge_id
        WHERE kt.tag_id = #{tagId}
          AND k.status = 1
          AND (k.deleted IS NULL OR k.deleted = 0)
    </select>

</mapper>
