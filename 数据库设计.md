# 数据库设计

> 数据库：MySQL 8.0+　|　字符集：utf8mb4　|　排序规则：utf8mb4_unicode_ci
> ORM：MyBatis-Plus，启用逻辑删除（`deleted = 1`）

---

## 1. 用户表 `sys_user`

### 1.1 表结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:---:|--------|------|
| `id` | BIGINT | ✅ | AUTO_INCREMENT | 主键 |
| `username` | VARCHAR(64) | ✅ | — | 用户名，唯一，用于登录 |
| `password` | VARCHAR(255) | ✅ | — | BCrypt 加密后的密码 |
| `email` | VARCHAR(128) | | NULL | 邮箱 |
| `phone` | VARCHAR(20) | | NULL | 手机号 |
| `nickname` | VARCHAR(64) | | NULL | 昵称（展示用） |
| `avatar` | VARCHAR(512) | | NULL | 头像 URL |
| `status` | TINYINT | | `1` | 状态：0=禁用，1=启用，2=锁定 |
| `last_login_time` | DATETIME | | NULL | 最后登录时间 |
| `last_login_ip` | VARCHAR(64) | | NULL | 最后登录 IP |
| `created_at` | DATETIME | | `CURRENT_TIMESTAMP` | 创建时间（自动填充） |
| `updated_at` | DATETIME | | `CURRENT_TIMESTAMP` | 更新时间（自动填充） |
| `deleted` | TINYINT | | `0` | 逻辑删除：0=未删除，1=已删除 |

### 1.2 索引

| 索引 | 字段 | 说明 |
|------|------|------|
| PRIMARY | `id` | 主键 |
| UNIQUE | `username` | 用户名唯一 |
| INDEX | `email` | 邮箱查询加速 |

---

## 2. 角色表 `sys_role`

### 2.1 表结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:---:|--------|------|
| `id` | BIGINT | ✅ | AUTO_INCREMENT | 主键 |
| `role_code` | VARCHAR(32) | ✅ | — | 角色编码，唯一，如 `ROLE_ADMIN` |
| `role_name` | VARCHAR(32) | ✅ | — | 角色名称，如 `管理员` |
| `description` | VARCHAR(255) | | NULL | 角色描述 |
| `sort` | INT | | `0` | 排序号 |
| `created_at` | DATETIME | | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | DATETIME | | `CURRENT_TIMESTAMP` | 更新时间 |
| `deleted` | TINYINT | | `0` | 逻辑删除 |

### 2.2 索引

| 索引 | 字段 | 说明 |
|------|------|------|
| PRIMARY | `id` | 主键 |
| UNIQUE | `role_code` | 角色编码唯一 |

---

## 3. 用户角色关联表 `sys_user_role`

### 3.1 表结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:---:|--------|------|
| `id` | BIGINT | ✅ | AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT | ✅ | — | 用户 ID |
| `role_id` | BIGINT | ✅ | — | 角色 ID |

### 3.2 索引

| 索引 | 字段 | 说明 |
|------|------|------|
| PRIMARY | `id` | 主键 |
| UNIQUE | `(user_id, role_id)` | 联合唯一，防止重复分配 |
| INDEX | `user_id` | 按用户查角色 |
| INDEX | `role_id` | 按角色查用户 |

---

## 4. 权限表 `sys_permission`

> 当前颗粒度只到用户 CRUD，后续按需追加。

### 4.1 表结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:---:|--------|------|
| `id` | BIGINT | ✅ | AUTO_INCREMENT | 主键 |
| `perm_code` | VARCHAR(64) | ✅ | — | 权限编码，如 `user:create` |
| `perm_name` | VARCHAR(64) | ✅ | — | 权限名称，如 `新增用户` |
| `description` | VARCHAR(255) | | NULL | 权限描述 |
| `created_at` | DATETIME | | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | DATETIME | | `CURRENT_TIMESTAMP` | 更新时间 |
| `deleted` | TINYINT | | `0` | 逻辑删除 |

### 4.2 索引

| 索引 | 字段 | 说明 |
|------|------|------|
| PRIMARY | `id` | 主键 |
| UNIQUE | `perm_code` | 权限编码唯一 |

---

## 5. 角色权限关联表 `sys_role_permission`

### 5.1 表结构

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:---:|--------|------|
| `id` | BIGINT | ✅ | AUTO_INCREMENT | 主键 |
| `role_id` | BIGINT | ✅ | — | 角色 ID |
| `perm_id` | BIGINT | ✅ | — | 权限 ID |

### 5.2 索引

| 索引 | 字段 | 说明 |
|------|------|------|
| PRIMARY | `id` | 主键 |
| UNIQUE | `(role_id, perm_id)` | 联合唯一，防止重复授权 |
| INDEX | `role_id` | 按角色查权限 |
| INDEX | `perm_id` | 按权限查角色 |

---

## 6. ER 关系

```
┌──────────┐      ┌────────────────┐      ┌──────────┐
│ sys_user │      │ sys_user_role  │      │ sys_role │
├──────────┤      ├────────────────┤      ├──────────┤
│ id       │──┐   │ user_id (FK)   │   ┌──│ id       │
│ username │  ├──>│ role_id (FK)   │<──┘  │ role_code│
│ ...      │  │   └────────────────┘      │ ...      │
└──────────┘  │                           └──────────┘
              │                                    │
              │                           ┌────────┴───────────┐
              │                           │ sys_role_permission│
              │                           ├────────────────────┤
              │           ┌──────────┐    │ role_id (FK)       │
              │           │sys_perm  │<───│ perm_id (FK)       │
              │           ├──────────┤    └────────────────────┘
              └── ? ──────│ perm_code│
                (无直接关联)│ ...     │
                          └──────────┘
```

---

## 7. 权限分配策略

| 权限 | 编码 | ADMIN | INTERVIEWER | CANDIDATE | USER |
|------|------|:---:|:---:|:---:|:---:|
| 查看用户列表 | `user:list` | ✅ | ✅ | — | — |
| 查看用户详情 | `user:view` | ✅ | ✅ | — | — |
| 新增用户 | `user:create` | ✅ | — | — | — |
| 编辑用户 | `user:edit` | ✅ | — | — | — |
| 删除用户 | `user:delete` | ✅ | — | — | — |

> Spring Security 通过 `hasAuthority('user:create')` 控制细粒度权限，`hasRole('ADMIN')` 控制角色级访问。

---

## 8. DDL

```sql
-- ============================================================
-- 1. 用户表
-- ============================================================
CREATE TABLE sys_user (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY      COMMENT '主键',
    username        VARCHAR(64)  NOT NULL                  COMMENT '用户名',
    password        VARCHAR(255) NOT NULL                  COMMENT 'BCrypt加密密码',
    email           VARCHAR(128) DEFAULT NULL              COMMENT '邮箱',
    phone           VARCHAR(20)  DEFAULT NULL              COMMENT '手机号',
    nickname        VARCHAR(64)  DEFAULT NULL              COMMENT '昵称',
    avatar          VARCHAR(512) DEFAULT NULL              COMMENT '头像URL',
    status          TINYINT      DEFAULT 1                 COMMENT '状态:0禁用,1启用,2锁定',
    last_login_time DATETIME     DEFAULT NULL              COMMENT '最后登录时间',
    last_login_ip   VARCHAR(64)  DEFAULT NULL              COMMENT '最后登录IP',
    created_at      DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
                                                           COMMENT '更新时间',
    deleted         TINYINT      DEFAULT 0                 COMMENT '逻辑删除',
    UNIQUE KEY uk_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统用户表';

-- ============================================================
-- 2. 角色表
-- ============================================================
CREATE TABLE sys_role (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY      COMMENT '主键',
    role_code   VARCHAR(32)  NOT NULL                  COMMENT '角色编码',
    role_name   VARCHAR(32)  NOT NULL                  COMMENT '角色名称',
    description VARCHAR(255) DEFAULT NULL              COMMENT '角色描述',
    sort        INT          DEFAULT 0                 COMMENT '排序号',
    created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at  DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
                                                       COMMENT '更新时间',
    deleted     TINYINT      DEFAULT 0                 COMMENT '逻辑删除',
    UNIQUE KEY uk_role_code (role_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统角色表';

-- ============================================================
-- 3. 用户角色关联表
-- ============================================================
CREATE TABLE sys_user_role (
    id      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
    user_id BIGINT NOT NULL                    COMMENT '用户ID',
    role_id BIGINT NOT NULL                    COMMENT '角色ID',
    UNIQUE KEY uk_user_role (user_id, role_id),
    INDEX idx_user_id (user_id),
    INDEX idx_role_id (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户角色关联表';

-- ============================================================
-- 4. 权限表
-- ============================================================
CREATE TABLE sys_permission (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY      COMMENT '主键',
    perm_code   VARCHAR(64)  NOT NULL                  COMMENT '权限编码',
    perm_name   VARCHAR(64)  NOT NULL                  COMMENT '权限名称',
    description VARCHAR(255) DEFAULT NULL              COMMENT '权限描述',
    created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at  DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
                                                       COMMENT '更新时间',
    deleted     TINYINT      DEFAULT 0                 COMMENT '逻辑删除',
    UNIQUE KEY uk_perm_code (perm_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统权限表';

-- ============================================================
-- 5. 角色权限关联表
-- ============================================================
CREATE TABLE sys_role_permission (
    id      BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
    role_id BIGINT NOT NULL                    COMMENT '角色ID',
    perm_id BIGINT NOT NULL                    COMMENT '权限ID',
    UNIQUE KEY uk_role_perm (role_id, perm_id),
    INDEX idx_role_id (role_id),
    INDEX idx_perm_id (perm_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色权限关联表';

-- ============================================================
-- 初始数据
-- ============================================================
INSERT INTO sys_role (role_code, role_name, sort) VALUES
('ROLE_ADMIN',       '管理员',  1),
('ROLE_INTERVIEWER', '面试官',  2),
('ROLE_CANDIDATE',   '候选人',  3),
('ROLE_USER',        '普通用户', 4);

INSERT INTO sys_permission (perm_code, perm_name) VALUES
('user:list',   '查看用户列表'),
('user:view',   '查看用户详情'),
('user:create', '新增用户'),
('user:edit',   '编辑用户'),
('user:delete', '删除用户');

-- ADMIN 拥有全部用户 CRUD 权限
INSERT INTO sys_role_permission (role_id, perm_id)
SELECT r.id, p.id FROM sys_role r, sys_permission p
WHERE r.role_code = 'ROLE_ADMIN';

-- INTERVIEWER 只能查看用户
INSERT INTO sys_role_permission (role_id, perm_id)
SELECT r.id, p.id FROM sys_role r, sys_permission p
WHERE r.role_code = 'ROLE_INTERVIEWER'
  AND p.perm_code IN ('user:list', 'user:view');
```
